# DiaryVaultMVP (FULL)

This is a **complete Gradle Android project** (without Android Studio GUI requirement).

## Build (Codespaces)
```bash
./gradlew assembleDebug
```

APK output:
`app/build/outputs/apk/debug/app-debug.apk`
